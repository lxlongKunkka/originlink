import { Context, Handler } from 'hydrooj';

export function apply(ctx: Context) {
  // This plugin primarily works by overriding the problem_sidebar_normal.html template
  // which is placed in the templates/partials directory.
  // HydroOJ should automatically pick up templates from the plugin's templates directory.
}
